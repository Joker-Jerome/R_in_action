# data input
df <- data.frame(age = numeric(0), name = character(0), weight = numeric(0))
edit(df)

# read table
# string will be converted to "factor" type
# using stringsAsFactors=FALSE

# read the file on the internet
url()

# read the excel file
install.packages("RODBC")
library(RODBC)

